package uk.tldcode.bots;

import java.util.Locale;

public class CommandConstants {
    public static final String JOIN = "join".toLowerCase(Locale.ROOT);
    public static final String LEAVE = "leave".toLowerCase(Locale.ROOT);
    public static final String SET_CHANNEL = "setchannel".toLowerCase(Locale.ROOT);
}
